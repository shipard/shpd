// Canary fixture used by tests/Unit/Core/Security/CanaryTableTest.php
// to prove that the encrypted_text infrastructure is end-to-end usable
// without depending on real consumers (AI backends etc.).
{
    "id": "test.secrets",
    "name": "Test Secrets",
    "description": "Canary module: core_test_secrets table with one encrypted_text column.",

    "dependencies": [],

    "tables": [
        "core_test_secrets"
    ],

    "documentClasses": [
        {
            "table": "core_test_secrets",
            "class": "Shipard\\Tests\\Fixtures\\Module\\Test\\Secrets\\TestSecretDocument"
        }
    ]
}
