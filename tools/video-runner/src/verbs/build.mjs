/** Verb `build` — implementace přijde v kroku 7 zadání spike. */
import { UserError } from '../errors.mjs';

export default async function build() {
  throw new UserError('Verb `build` zatím není implementovaný (krok 7).');
}
