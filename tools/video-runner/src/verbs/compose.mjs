/** Verb `compose` — implementace přijde v kroku 7 zadání spike. */
import { UserError } from '../errors.mjs';

export default async function compose() {
  throw new UserError('Verb `compose` zatím není implementovaný (krok 7).');
}
