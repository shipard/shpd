/** Verb `login` — implementace přijde v kroku 3 zadání spike. */
import { UserError } from '../errors.mjs';

export default async function login() {
  throw new UserError('Verb `login` zatím není implementovaný (krok 3).');
}
