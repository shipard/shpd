/** Verb `record` — implementace přijde v kroku 5 zadání spike. */
import { UserError } from '../errors.mjs';

export default async function record() {
  throw new UserError('Verb `record` zatím není implementovaný (krok 5).');
}
